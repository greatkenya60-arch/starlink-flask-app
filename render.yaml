services:
  - type: web
    name: your-service-name
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -k sync -b 0.0.0.0:$PORT flask_app:app
    healthCheckPath: /